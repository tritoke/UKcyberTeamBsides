<html>
  <head>
    <title>Anime Quote Adventure</title>
    <style>
      body {
        font-family: 'Arial', sans-serif;
        background: linear-gradient(to bottom, #f0f8ff, #e6e6fa);
        color: #333;
        margin: 0;
        padding: 20px;
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
      }
      .container {
        background: #fff;
        border-radius: 15px;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
        padding: 30px;
        max-width: 500px;
        text-align: center;
      }
      h1 {
        color: #ff6f61;
        font-size: 2em;
        margin-bottom: 15px;
      }
      p {
        font-size: 1.1em;
        color: #555;
        margin: 10px 0;
      }
      form {
        margin: 20px 0;
      }
      input[type="text"] {
        padding: 10px;
        font-size: 1em;
        border: 2px solid #4682b4;
        border-radius: 5px;
        width: 80%;
        margin-bottom: 10px;
      }
      input[type="submit"] {
        padding: 10px 20px;
        background: #ff6f61;
        color: #fff;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        transition: background 0.3s;
      }
      input[type="submit"]:hover {
        background: #e55a50;
      }
      .hint {
        font-style: italic;
        color: #4682b4;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Anime Quotes</h1>
      <p>Enter your name:</p>
      <form method="POST" action="/quote">
        <input type="text" name="username" placeholder="Your name" />
        <input type="submit" value="Get Quote" />
      </form>
      <p class="hint">Hint: Only a true anime hero can uncover the hidden truth!</p>
    </div>
  </body>
</html>